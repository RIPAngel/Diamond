#include <bits/stdc++.h>
using namespace std;
const int XX = 2 * 2 * 6974;
string a[XX];
int n;

void __init__ () {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
}

int main () {
    __init__();
    cin >> n;
    for (int i = 0; i < n; ++i) {
        cin >> a[i];
    }
}