#include <iostream>
using namespace std;
const int IND = 1e5 + 5;
int n, k, q, arr[IND];

int main () {
    cin >> n >> k >> q;
    while (q--) {
        int x, y, res = 0;
        cin >> x >> y;
    }
}